import { StyleSheet, Text, View, SafeAreaView } from 'react-native'
import React from 'react'

export default function hello1() {
    return (
        <SafeAreaView>
      const display = () => (
            <TouchableOpacity style={{ backgroundColor: '#e5e5', alignItems: 'center' }}>
                <Text style={{ fontSize: 56 }}>display me</Text>
            </TouchableOpacity>
            )
        </SafeAreaView>
    )
}

const styles = StyleSheet.create({})